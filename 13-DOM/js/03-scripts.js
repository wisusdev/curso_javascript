// TODO: Seleccionar elementos por su id
let formulario = document.getElementById('formulario');
console.log(formulario);

let noExiste = document.getElementById('noExiste');
console.log(noExiste);

// Añadir un ID 2 veces, seleccionara el primero que encuentre...
// Existen otro par de selectores que hoy en día son los más comunes, tienen una sintaxis muy similar a la de CSS, así que vamos viendolos en el siguiente video...